<configuration>
    <appender name="FLUENTBIT" class="ch.qos.logback.classic.net.SocketAppender">
        <remoteHost>localhost</remoteHost>
        <port>24224</port> 
        <!-- <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder> -->
    </appender>

    <root level="DEBUG">
        <appender-ref ref="FLUENTBIT" />
    </root>
</configuration>